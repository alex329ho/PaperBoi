# @react-native-firebase/app (local stub)

This local stub satisfies Expo config plugin resolution in environments where the upstream package
is unavailable from the registry. It exposes a no-op config plugin so `expo config` and related
commands can run without errors. Replace with the official package when registry access is available.
