import { typography } from '../theme/typography';

// Font references for use outside of theme-aware components.
export const fonts = {
  families: typography.fontFamily,
  sizes: typography.sizes,
  lineHeights: typography.lineHeights,
};
